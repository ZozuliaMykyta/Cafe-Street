import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

const cafe = ReactDOM.createRoot(document.getElementById('cafe'));
cafe.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

